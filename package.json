{
  "name": "skillmatch-root",
  "version": "1.0.0",
  "description": "SkillMatch Project Root Runner",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "backend": "java -jar backend/target/backend-0.0.1-SNAPSHOT.jar",
    "ai": "ai-service\\venv\\Scripts\\python ai-service/app.py",
    "maildev": "maildev",
    "start-all": "concurrently \"npm run maildev\" \"npm run dev\" \"npm run backend\" \"npm run ai\"",
    "install-all": "npm install --prefix frontend && mvn -f backend/pom.xml install && ai-service/venv/Scripts/pip install -r ai-service/requirements.txt"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "maildev": "^2.2.1"
  }
}
